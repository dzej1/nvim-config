return {
  "folke/snacks.nvim",
  opts = {
    dashboard = {
      preset = {
        header = [[

        ]],
      },
    },
  },
  -- opts = function(_, dashboard)

  --   logo = string.rep("\n", 8) .. logo .. "\n\n"
  --   dashboard.config.header = vim.split(logo, "\n")
  -- end,
}
